
services:
  flask-app:
    build: .
    container_name: oahu-food-finder
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    restart: unless-stopped